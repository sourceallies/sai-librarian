version: 0.2
env:
  variables:
    CI: "true"
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm ci
  build:
    commands:
      - npm run eslint
      - npm test
      - npm run build
artifacts:
  base-directory: build
  files:
    - '**/*'